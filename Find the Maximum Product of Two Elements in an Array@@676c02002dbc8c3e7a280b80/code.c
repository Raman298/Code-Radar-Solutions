#include<stdio.h>
#include<stdlib.h>
int sumarray(int arr[],int n){
    
    for(int i=0;i<n;i++){
        int b=abs(arr[i]);
        int max=1;
        int b=arr[i]*arr[i+1]
    }
}
int main(){
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    sumarray(arr,n);
    for(int i=0;i<n;i++){
        printf("%d ",arr[i]);
    }
}